export interface Project {
  id: string;
  title: string;
  description: string;
  image: string;
  tags: string[];
}

export interface Tool {
  id: string;
  name: string;
  description: string;
  icon: string;
}